
.. _model:

Models
======

A model groups together a module and loss function together with an optimizer: the three components allow the parameters in the module to be trained or fitted by minimizing the loss.
